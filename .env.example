# PostgreSQL Database Configuration
# Option 1: Use full connection string
DATABASE_URL=postgresql://user:password@host:5432/database_name

## Option 2: Use individual parameters (if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password
DB_SSL=false
DB_POOL_MAX=5

# JWT Authentication
# Generate with: openssl rand -hex 32 or npm run keygen
PRKEY=your-32-byte-hex-key-here

# Server Configuration
PORT=8080
NODE_ENV=development
